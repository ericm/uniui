---
name: Select
route: /components/select
menu: 2. Components
---

import {  SelectionGroup } from "../../src";
import Select from "../../src/components/Select";
import "../global.css";
import { Playground } from 'docz';
import PropsTable from 'react-docgen-props-table';


# Select Component

The `Select` Component comes with all of the HTML Attribute props for a `HTMLSelectElement` as well as:

<PropsTable
    props={
    {
    "options": {
      "type": {
        "name": `[{name: string; value: string | number}]`
      },
      "required": true,
      "description": `
Array of Option Objects used to dsplay key value options in element.
      `
    },
    "selectedIndex": {
      "type": {
        "name": `number`
      },
      "required": false,
      "description": `
Index of initially selected option.
      `
    },  
    "emptyEntry": {
      "type": {
        "name": `boolean`
      },
      "required": false,
      "description": `
If there should be an empty entry.
      `
    },  
    "native": {
      "type": {
        "name": `boolean`
      },
      "required": false,
      "description": `
If the select element should be native.
      `
    },  
    "subtitle": {
      "type": {
        "name": `string`
      },
      "required": false,
      "description": `
Subtitle of the field.
      `
    },
    "onChangeText": {
      "type": {
        "name": `(s: string) => void`
      },
      "required": false,
      "description": `
Function called when text is changed in input. Will give text as parameter s.
      `
    } }
} />


## Basic Usage

<Playground>

    <SelectionGroup>
        <Select options={[{name: "test", value: "test"},{name: "test2", value: "test2"}, {name: "test3", value: "test3"}]} subtitle="Select 1" />
        <Select options={[{name: "test", value: "test"}]} native={true} subtitle="Select 2" />
        <Select options={[{name: "test", value: "test"},{name: "test2", value: "test2"}, {name: "test3", value: "test3"}]} emptyEntry={false} subtitle="Select 3"/>
        <Select options={[
            {name: "aSelect 4", value: "test"},
            {name: "btest2", value: "test2"}, 
            {name: "ctest3", value: "test3"},
            {name: "dtest4", value: "test4"},
            {name: "etest5", value: "test5"},
            {name: "ftest6", value: "test6"},
            {name: "gtest7", value: "test7"},
            {name: "htest8", value: "test8"},
            {name: "test9", value: "test9"},
        ]} emptyEntry={false} />
    </SelectionGroup>

</Playground>
