---
name: Selection
route: /components/selection
menu: 2. Components
---

import {  SelectionGroup } from "../../src";
import Selection from "../../src/components/Selection";
import "../global.css";
import { Playground, Props } from 'docz';


# Selection Component

The `Selection` Component comes with all of the HTML Attribute props for a `HTMLInputElement` as well as:

<PropsTable
    props={
    {
    "type": {
      "type": {
        "name": `"radio" | "checkbox" | "switch"`
      },
      "required": true,
      "description": `
Type of the selection.
Radio can only be selected one at a time
Checkbox and switch are skinned checkboxes
      `
    },
    "checked": {
      "type": {
        "name": `boolean`
      },
      "required": false,
      "description": `
Default state of the input
      `
    },
    "label": {
      "type": {
        "name": `string`
      },
      "required": false,
      "description": `
Optional label next to input
      `
    },
    "onChangeValue": {
      "type": {
        "name": `(state: boolean) => void`
      },
      "required": false,
      "description": `
Callback function providing changed state
      `
    }
    }
} />

## Basic Radio Usage

<Playground>

    <SelectionGroup>
        <Selection type="radio" label="Radio button 1" />
        <Selection type="radio" label="Radio button 2" />
        <Selection type="radio" label="Radio button 3" />
    </SelectionGroup>

</Playground>

## Basic Checkbox Usage

<Playground>

    <SelectionGroup>
        <Selection type="checkbox" label="Checkbox 1" />
        <Selection type="checkbox" label="Checkbox 2" />
        <Selection type="checkbox" label="Checkbox 3" />
    </SelectionGroup>

</Playground>

## Basic Switch Usage

<Playground>

    <SelectionGroup>
        <Selection type="switch" label="Switch 1" />
        <Selection type="switch" label="Switch 2" />
        <Selection type="switch" label="Switch 3" />
    </SelectionGroup>

</Playground>
