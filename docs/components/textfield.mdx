---
name: Text Field
route: /components/textfield
menu: 2. Components
---

import "../global.css";
import  TextField  from "../../src/components/TextField";
import { Playground } from 'docz';
import PropsTable from 'react-docgen-props-table';


# TextField Component

The `TextField` Component comes with all of the HTML Attribute props for a `HTMLInputElement` as well as:

<PropsTable
    props={
    {
    "type": {
      "type": {
        "name": `"text" | "search" | "password" | "number"`
      },
      "required": false,
      "description": `
Type prop passed to input
      `
    },
    "subtitle": {
      "type": {
        "name": `string`
      },
      "required": false
    },
    "value": {
      "type": {
        "name": `string`
      },
      "required": false
    },
    "onChangeText": {
      "type": {
        "name": `(s: string) => void`
      },
      "required": false,
      "description": `
Callback function providing changed state
      `
    }
    }
} />

## TextField Usage

<Playground>

    <TextField 
        value="Text"
        subtitle="TextField"
    />
    <TextField 
        value="1234"
        subtitle="Phone Number"
        type="number"
    />
    <TextField 
        subtitle="Search"
        type="search"
    />
    <TextField 
        subtitle="Password"
        type="password"
    />

</Playground>
