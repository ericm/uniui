---
name: Button
route: /components/button
menu: 2. Components
---

import "../global.css";
import { Button } from "../../src/";
import { Playground } from 'docz';
import PropsTable from 'react-docgen-props-table';


# Button Component

The `Button` Component comes with all of the HTML Attribute props for a `HTMLButtonElement` as well as:

<PropsTable
    props={
    {
    "type": {
      "type": {
        "name": `"standard" | "flat" | "borderless"`
      },
      "required": false,
      "description": `
Type of the button.
Standard is a 3d effect block element.
Flat is a bordered modern button.
Borderless is exclusively a ripple element
      `
    }  }
} />

## Basic Usage

<Playground>

    <Button>Button</Button>
    <Button type="flat">Button</Button>
    <Button type="borderless">Button</Button>

</Playground>
